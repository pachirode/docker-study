### 挂载错误排查

查看内核是否支持 `overlayfs`
`lsmod | grep overlay`

内核查看错误信息
`dmesg | tail -20`

### 测试

##### 修改文件

`lower` 为底层目录，只提供数据，不能写；如果文件仅存在此处，但是修改了，会复制一份到上层
`upper` 为上层目录，可读可写

##### 删除文件

删除文件是在上层添加一个标记文件类型为 `c`，不会真正的删除 `lower` 中的文件

##### 添加文件

直接在 `upper` 中添加文件
