# 匿名管道

匿名管道是一种特殊的文件描述符，用于在父进程和子进程之间创建通信通道
可以使得一个进程输出成为另一个进程的输入，创建一个管道，将 `readPipe FD` 告知子进程，`writePipe FD` 告知父进程

- 管道有一个固定大小的缓存区，一般为 `4kb`
- 通道的流向是单向的

### 父进程数据读写

需要保证在写入之前启动子进程，如果提前写入大量数据导致管道阻塞，就会导致管道数据一直阻塞，永远不会被读取

### 子进程数据读取

先获取 `readPipe FD` 然后从中读取数据
