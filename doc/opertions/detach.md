实现容器后台运行

`runC` 提供一种 `detach` 功能，实现创建完容器，命令行退出，容器依旧可以继续运行
实现原理是，当父进程退出，孤儿进程会被 `init` 进程接收

# 查看运行中的容器

容器在脱离父进程进行后台运行，需要保存一些信息便于我们知道有哪些容器存在，已经运行状态等

### ps

将每次运行的数据保存到一个 `json` 中，记录此次容器启动的各种数据
变量目标文件夹下面的所有文件，将文件反序列化并整理关键信息打印

### logs

处理容器的运行状态等信息，还需要了解容器日志，因此需要将容器中的标准输出保存下来