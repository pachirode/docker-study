使用 `setns` 系统调用，根据指定的 `PID` 再次进入到指定的命名空间
对于 `Mount Namespace` 来说，一个具有多线程的进程是无法直接调用这个系统调用的，`Go` 启动程序会进入多线程状态，因此需要借助 `C`

### Cgo

用来调用 `C` 代码

### 流程

导入 `C` 代码，每次执行前使用单线程进入，为了不影响其他代码功能，使用环境变量来区分是否执行这段代码
使用 `exec` 命令运行，先设置环境变量，`/proc/self/exe` 创建一个子线程，此时可以识别环境变量，`C` 代码会切换线程