# Linux proc 文件系统

`/proc` 文件系统是由内核提供的，只包含系统运行时的信息
它只存在内存中，不占用磁盘空间
很多系统工具就是去简单的文件系统的某个文件内容

- 系统内存
- `mount` 设备信息

### 重要部分

- `/proc/N`
    - `PID` 为进程信息
- `/proc/N/cmdline`
    - 进程启动命令
- `/proc/N/environ`
    - 进程环境变量
- `/proc/N/fd`
    - 包含进程相关的所有文件描述符
- `/proc/self`
    - 链接当前正在运行的进程

